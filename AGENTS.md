# Bubble Shield — Agent Notes

A Fabric mod for **Minecraft 26.2** that adds a furnace-like "Bubble Shield Projector" block: insert fuel, open its GUI to whitelist players, pick 1 of 50 effects, and set the bubble diameter (8–200). The bubble is a server-authoritative spherical shield with per-effect surface shaders, inside behaviors, and in-bubble screen post-effects; arrows shrink it until it breaks (30-min cooldown). See `README.md` for the feature/user-facing overview and the standard `./gradlew` commands.

## Cursor Cloud specific instructions

- **Toolchain:** Minecraft 26.2 requires **JDK 25** (Temurin 25 is installed at `/usr/lib/jvm/jdk-25.0.3+9`). The startup update script installs it and persists `JAVA_HOME` in `~/.bashrc`. If a non-login shell lacks it, run `export JAVA_HOME=/usr/lib/jvm/jdk-25.0.3+9 && export PATH=$JAVA_HOME/bin:$PATH` before any `./gradlew` command. The build targets/`release`s Java 25, so an older JDK will fail.
- **First build is slow:** `./gradlew build` downloads Gradle 9.5.1 and Minecraft 26.2 on first run (several minutes); later builds are ~15–30s. There is no system-wide `gradle` — always use the wrapper `./gradlew`.
- **Primary verification is headless gametests:** `./gradlew runGameTest` runs the full server-side suite (shield lifecycle, arrow damage/shrink/cooldown, whitelist barrier + ownership, menu open, 50-effect validity, datapack/post-effect asset existence). This is the main correctness gate — use it, not just `build`. `./gradlew runServer` also boots a headless dedicated server, but the `run/` dir is git-ignored, so on a fresh VM the first `runServer` writes `run/eula.txt` with `eula=false` and exits — set it to `eula=true` once (e.g. `sed -i 's/eula=false/eula=true/' run/eula.txt`) and re-run. Loom's `configureTests { eula = true }` still auto-accepts the gametest EULA, so `runGameTest` needs no manual step. Once booted, exercise the mod server-side without a GPU via the console: `forceload add 0 0`, `setblock 0 -60 0 bubbleshield:bubble_shield_projector`, then `data get block 0 -60 0` shows the projector block-entity state (health, target_radius, fuel_seconds, whitelist, effect_id).
- **No GPU on this VM — the client cannot be visually verified.** `./gradlew runClient` needs real OpenGL and will not render here, so the bubble **surface shaders** and **in-bubble screen post-effects** cannot be seen/tested in Cursor Cloud. Verify shader/rendering changes by: (1) `glslangValidator` on the GLSL if available — note it is NOT preinstalled on the Cursor Cloud VM, and both `apt` (archive.ubuntu.com) and the glslang GitHub-release CDN are egress-blocked here, so this step usually cannot run; when it is available, expand `#moj_import` includes from the vanilla client jar first, (2) structural diffing against vanilla 26.2 shaders, and (3) build + `runGameTest` (asset-existence + JSON-parse tests). State this limitation honestly rather than claiming visual verification.
- **26.2 is UNOBFUSCATED / Mojang official mappings (NOT Yarn).** Old Yarn-name tutorials are wrong here (e.g. it's `Creeper`/`CreativeModeTabEvents`, not `CreeperEntity`/`ItemGroupEvents`; `AbstractArrow` is in `...projectile.arrow`). The build uses the `net.fabricmc.fabric-loom` plugin with plain `implementation` deps (no `modImplementation`/`remapJar`). When unsure of an API, decompile with `./gradlew genSources` and read the sources jars under `.gradle/loom-cache/minecraftMaven/**/*-sources.jar` — always confirm signatures there before using them.
- **26.2 API shape gotchas** (verified in this codebase): block-entity NBT uses `ValueInput`/`ValueOutput` (not `CompoundTag` params); GUIs are extractor-based (`extractBackground`/`extractLabels`, not `renderBg`); world rendering is submit-based (`LevelRenderEvents.COLLECT_SUBMITS` + `submitCustomGeometry`; `WorldRenderEvents` does not exist); datapack folders are singular (`loot_table`, `recipe`, `tags/block`).
- **Gametests live in the main source set** (`com.bubbleshield.gametest.ShieldGameTests`, registered via the `fabric-gametest` entrypoint) with `fabricApi { configureTests { createSourceSet = false; eula = true } }` in `build.gradle`. Post-effect/screenfx assets are intentionally under `src/main/resources` (not `src/client/resources`) so the headless gametest server classpath can see them for the asset-existence test.
